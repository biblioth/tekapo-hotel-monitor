name: Hourly hotel monitor 48

on:
  schedule:
    - cron: "48 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    uses: ./.github/workflows/monitor-core.yml
    with:
      trigger_kind: ${{ github.event_name }}
    secrets: inherit
