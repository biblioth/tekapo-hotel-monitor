name: Cloud-triggered browser validation

on:
  repository_dispatch:
    types: [lakewatch-browser-check]
  workflow_dispatch:

jobs:
  monitor:
    uses: ./.github/workflows/monitor-core.yml
    with:
      trigger_kind: ${{ github.event_name }}
    secrets: inherit
