name: Hourly hotel monitor 38

on:
  schedule:
    - cron: "38 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    uses: ./.github/workflows/monitor-core.yml
    with:
      trigger_kind: ${{ github.event_name }}
    secrets: inherit
