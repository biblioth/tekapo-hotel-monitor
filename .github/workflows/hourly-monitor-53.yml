name: Hourly hotel monitor backup 53

on:
  schedule:
    - cron: "53 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    uses: ./.github/workflows/monitor-core.yml
    with:
      trigger_kind: ${{ github.event_name }}
    secrets: inherit
