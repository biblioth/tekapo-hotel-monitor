name: Hourly hotel monitor 33

on:
  schedule:
    - cron: "33 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    uses: ./.github/workflows/monitor-core.yml
    with:
      trigger_kind: ${{ github.event_name }}
    secrets: inherit
