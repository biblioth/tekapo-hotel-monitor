name: Legacy hotel monitor (manual fallback)

on:
  workflow_dispatch:

jobs:
  monitor:
    uses: ./.github/workflows/monitor-core.yml
    with:
      trigger_kind: legacy-manual-fallback
    secrets: inherit
